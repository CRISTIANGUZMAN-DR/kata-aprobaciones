spring:
  datasource:
    url: jdbc:h2:mem:aprobaciones;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    com.kata.aprobaciones: DEBUG
